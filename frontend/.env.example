# ============================================================================
# Frontend environment variables — copy to .env.local for local development
# ============================================================================
# Public vars (NEXT_PUBLIC_*) are inlined at build time and visible in the browser.
# All other vars are server-side only (used by Next.js API routes / middleware).
# NEVER put secrets in NEXT_PUBLIC_ variables.
# ============================================================================

# ── Public ──────────────────────────────────────────────────────────────────
# URL that the browser uses to reach the Next.js app itself
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Version tag (injected by CI, e.g. git describe --tags)
NEXT_PUBLIC_APP_VERSION=dev

# Optional: Sentry DSN for client-side error reporting
# NEXT_PUBLIC_SENTRY_DSN=

# ── Server-side only ─────────────────────────────────────────────────────────
# URL that Next.js API routes use to reach the FastAPI backend (server-to-server)
BACKEND_URL=http://localhost:8000

# Cookie security — set to "true" in production (requires HTTPS)
# NODE_ENV=production

# Optional: Sentry DSN for server-side error reporting (can differ from client)
# SENTRY_DSN=
# SENTRY_ORG=
# SENTRY_PROJECT=
# SENTRY_AUTH_TOKEN=       # only needed for source-map upload during build
