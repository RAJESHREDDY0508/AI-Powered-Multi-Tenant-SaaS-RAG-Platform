# Core
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-multipart>=0.0.9       # required by FastAPI for Form() / File() multipart parsing

# Database
sqlalchemy>=2.0.30
asyncpg>=0.29.0              # async PostgreSQL driver
alembic>=1.13.0              # database migrations

# AWS
aioboto3>=13.0.0             # async boto3 wrapper for S3, KMS, IAM

# Vector Stores
pinecone>=3.2.0              # Pinecone client v3+
weaviate-client>=4.6.0       # Weaviate v4 client

# LangChain — RAG orchestration
langchain>=0.2.0
langchain-core>=0.2.0
langchain-openai>=0.1.0         # OpenAI embeddings + chat
langchain-text-splitters>=0.2.0 # RecursiveCharacterTextSplitter
langchain-community>=0.2.0      # LongContextReorder, ChatOllama
langchain-aws>=0.1.0            # ChatBedrock (AWS Bedrock Claude)

# Auth — JWT / OIDC
python-jose[cryptography]>=3.3.0   # JWT decode + JWKS verification
httpx>=0.27.0                       # async HTTP (JWKS fetch)

# Task Queue — Celery + Broker
celery[redis]>=5.4.0         # async task queue with Redis result backend
kombu>=5.3.0                 # AMQP message routing (RabbitMQ/Redis)
redis>=5.0.0                 # Redis client (result backend + dev broker)

# Document Parsing — base
pypdf>=4.2.0                 # PDF text extraction (pure Python, no poppler dep)
python-docx>=1.1.0           # DOCX text extraction

# Document Parsing — OCR strategy pipeline (app/processing/)
PyMuPDF>=1.24.0              # fast native PDF text layer (PyMuPDFExtractor)
unstructured[pdf]>=0.14.0    # on-premise OCR for scanned PDFs (UnstructuredExtractor)
boto3>=1.34.0                # sync boto3 for AWS Textract (TextractExtractor)
psycopg2-binary>=2.9.9       # sync PostgreSQL driver for Celery retry scanner

# NLP — Semantic Chunking (app/processing/chunking.py)
spacy>=3.7.0                 # sentence segmentation (SemanticChunker)
# After install run: python -m spacy download en_core_web_sm

# OpenAI — Embedding Pipeline (app/processing/embeddings.py)
openai>=1.30.0               # AsyncOpenAI for batch text-embedding-3-small

# File type detection
python-magic>=0.4.27         # libmagic bindings for MIME sniffing (Linux/Mac)
# python-magic-bin>=0.4.14   # Windows: uncomment this, comment python-magic above

# Hybrid Retrieval — Phase 3.1
rank-bm25>=0.2.2             # BM25 sparse retrieval (TenantBM25Index)
cohere>=5.0.0                # Cohere ReRank cross-encoder (CohereReranker)

# LLM Providers — Phase 4 (multi-provider gateway)
anthropic>=0.25.0            # Anthropic Claude direct API (optional — prefer Bedrock)
# langchain-community provides ChatOllama for local Llama 3

# Observability — Phase 5
structlog>=24.1.0                         # structured JSON logging
prometheus-fastapi-instrumentator>=6.1.0  # /metrics endpoint (Prometheus)
langsmith>=0.1.0                          # LangSmith tracing (optional, LANGCHAIN_API_KEY)
# arize-phoenix-otel>=3.0.0              # Arize Phoenix OTEL tracing (uncomment to enable)
# opentelemetry-sdk>=1.24.0             # Generic OTEL (uncomment for Jaeger/Datadog)

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0             # test coverage reporting (--cov flag)
pytest-timeout>=2.3.0         # per-test timeout (prevents SSE stream hangs in CI)
