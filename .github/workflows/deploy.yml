##############################################################
# Auto-Deploy to AWS
# Triggers every time you push code to the main branch.
#
# What it does:
#   1. Runs your tests
#   2. Builds Docker images
#   3. Pushes images to AWS ECR
#   4. Runs database migrations
#   5. Deploys updated containers to ECS
##############################################################

name: Deploy to AWS

on:
  push:
    branches: [main]          # Auto-deploy on every push to main
  workflow_dispatch:          # Also allow manual trigger from GitHub UI

jobs:

  # ════════════════════════════════════════════════════════════
  # JOB 1 — Run Tests
  # ════════════════════════════════════════════════════════════
  test:
    name: 🧪 Run Tests
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER:     rag
          POSTGRES_PASSWORD: rag
          POSTGRES_DB:       ragplatform_test
        ports: ["5432:5432"]
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10

      redis:
        image: redis:7-alpine
        ports: ["6379:6379"]
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install Python dependencies
        working-directory: backend
        run: pip install -r requirements.txt

      - name: Run tests
        working-directory: backend
        env:
          DATABASE_URL:         postgresql+asyncpg://rag:rag@localhost:5432/ragplatform_test
          REDIS_URL:            redis://localhost:6379/0
          AUTH_ISSUER:          https://test.auth0.com/
          AUTH_AUDIENCE:        test-audience
          OPENAI_API_KEY:       sk-test-placeholder
          S3_BUCKET:            test-bucket
          AWS_REGION:           us-east-1
          VECTOR_STORE_BACKEND: weaviate
          WEAVIATE_HOST:        localhost
          WEAVIATE_PORT:        "8080"
          WEAVIATE_API_KEY:     ""
          APP_ENV:              test
          DEBUG:                "true"
        run: python -m pytest -m "unit or integration" --tb=short -q --timeout=60

  # ════════════════════════════════════════════════════════════
  # JOB 2 — Build and Push Docker Images
  # ════════════════════════════════════════════════════════════
  build:
    name: 🐳 Build & Push Images
    runs-on: ubuntu-latest
    needs: test                # Only runs if tests pass

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ vars.AWS_REGION }}

      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set up Docker Buildx (for faster builds with caching)
        uses: docker/setup-buildx-action@v3

      - name: Build and push API image
        uses: docker/build-push-action@v5
        with:
          context:    ./backend
          file:       ./backend/Dockerfile
          target:     api
          push:       true
          tags: |
            ${{ vars.ECR_API_URL }}:${{ github.sha }}
            ${{ vars.ECR_API_URL }}:latest
          cache-from: type=gha,scope=api
          cache-to:   type=gha,scope=api,mode=max

      - name: Build and push Frontend image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push:    true
          build-args: |
            NEXT_PUBLIC_APP_VERSION=${{ github.sha }}
          tags: |
            ${{ vars.ECR_FRONTEND_URL }}:${{ github.sha }}
            ${{ vars.ECR_FRONTEND_URL }}:latest
          cache-from: type=gha,scope=frontend
          cache-to:   type=gha,scope=frontend,mode=max

  # ════════════════════════════════════════════════════════════
  # JOB 3 — Run Database Migrations
  # ════════════════════════════════════════════════════════════
  migrate:
    name: 🗄️ Run Migrations
    runs-on: ubuntu-latest
    needs: build               # Only runs after images are pushed

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ vars.AWS_REGION }}

      - name: Run database migrations
        run: |
          echo "Starting migration task in ECS..."

          TASK_ARN=$(aws ecs run-task \
            --cluster  "${{ vars.ECS_CLUSTER }}" \
            --task-definition "${{ vars.ECS_MIGRATION_TASK_DEF }}" \
            --launch-type FARGATE \
            --network-configuration "awsvpcConfiguration={subnets=[${{ vars.ECS_PRIVATE_SUBNET }}],securityGroups=[${{ vars.ECS_SECURITY_GROUP }}],assignPublicIp=DISABLED}" \
            --overrides "{\"containerOverrides\":[{\"name\":\"migration\",\"image\":\"${{ vars.ECR_API_URL }}:${{ github.sha }}\"}]}" \
            --query 'tasks[0].taskArn' \
            --output text)

          echo "Task ARN: $TASK_ARN"
          echo "Waiting for migrations to complete..."

          aws ecs wait tasks-stopped \
            --cluster "${{ vars.ECS_CLUSTER }}" \
            --tasks   "$TASK_ARN"

          EXIT_CODE=$(aws ecs describe-tasks \
            --cluster "${{ vars.ECS_CLUSTER }}" \
            --tasks   "$TASK_ARN" \
            --query   'tasks[0].containers[0].exitCode' \
            --output  text)

          echo "Migration exit code: $EXIT_CODE"

          if [ "$EXIT_CODE" != "0" ]; then
            echo "❌ Migration FAILED (exit code $EXIT_CODE)"
            echo "Check logs at: AWS Console → CloudWatch → Log groups → /ecs/.../migration"
            exit 1
          fi

          echo "✅ Migrations completed successfully"

  # ════════════════════════════════════════════════════════════
  # JOB 4 — Deploy to ECS
  # ════════════════════════════════════════════════════════════
  deploy:
    name: 🚀 Deploy to AWS
    runs-on: ubuntu-latest
    needs: migrate             # Only runs after migrations succeed

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ vars.AWS_REGION }}

      # ── Deploy API ─────────────────────────────────────────
      - name: Get current API task definition
        run: |
          aws ecs describe-task-definition \
            --task-definition "${{ vars.ECS_CLUSTER }}-api" \
            --query taskDefinition \
            --output json > api-task-def.json

      - name: Update API image in task definition
        id: api-task-def
        uses: aws-actions/amazon-ecs-render-task-definition@v1
        with:
          task-definition: api-task-def.json
          container-name:  api
          image:           ${{ vars.ECR_API_URL }}:${{ github.sha }}

      - name: Deploy API service
        uses: aws-actions/amazon-ecs-deploy-task-definition@v1
        with:
          task-definition:             ${{ steps.api-task-def.outputs.task-definition }}
          service:                     ${{ vars.ECS_API_SERVICE }}
          cluster:                     ${{ vars.ECS_CLUSTER }}
          wait-for-service-stability:  true

      # ── Deploy Worker ──────────────────────────────────────
      - name: Get current Worker task definition
        run: |
          aws ecs describe-task-definition \
            --task-definition "${{ vars.ECS_CLUSTER }}-worker" \
            --query taskDefinition \
            --output json > worker-task-def.json

      - name: Update Worker image in task definition
        id: worker-task-def
        uses: aws-actions/amazon-ecs-render-task-definition@v1
        with:
          task-definition: worker-task-def.json
          container-name:  worker
          image:           ${{ vars.ECR_API_URL }}:${{ github.sha }}

      - name: Deploy Worker service
        uses: aws-actions/amazon-ecs-deploy-task-definition@v1
        with:
          task-definition:             ${{ steps.worker-task-def.outputs.task-definition }}
          service:                     ${{ vars.ECS_WORKER_SERVICE }}
          cluster:                     ${{ vars.ECS_CLUSTER }}
          wait-for-service-stability:  false

      # ── Deploy Frontend ────────────────────────────────────
      - name: Get current Frontend task definition
        run: |
          aws ecs describe-task-definition \
            --task-definition "${{ vars.ECS_CLUSTER }}-frontend" \
            --query taskDefinition \
            --output json > frontend-task-def.json

      - name: Update Frontend image in task definition
        id: frontend-task-def
        uses: aws-actions/amazon-ecs-render-task-definition@v1
        with:
          task-definition: frontend-task-def.json
          container-name:  frontend
          image:           ${{ vars.ECR_FRONTEND_URL }}:${{ github.sha }}

      - name: Deploy Frontend service
        uses: aws-actions/amazon-ecs-deploy-task-definition@v1
        with:
          task-definition:             ${{ steps.frontend-task-def.outputs.task-definition }}
          service:                     ${{ vars.ECS_FRONTEND_SERVICE }}
          cluster:                     ${{ vars.ECS_CLUSTER }}
          wait-for-service-stability:  true

      # ── Summary ───────────────────────────────────────────
      - name: Print deployment summary
        run: |
          echo "## ✅ Deployment Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Service  | Image Tag |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|-----------|" >> $GITHUB_STEP_SUMMARY
          echo "| API      | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Worker   | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Frontend | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Commit: ${{ github.event.head_commit.message }}" >> $GITHUB_STEP_SUMMARY
